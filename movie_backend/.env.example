# Flask backend environment (do not commit real values)
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# Prefer a SERVICE ROLE key on the server if you plan to perform privileged operations
SUPABASE_KEY=YOUR_SERVICE_ROLE_OR_ANON_KEY

# Optional: site URL (used for CORS or redirects)
SITE_URL=http://localhost:3001/
